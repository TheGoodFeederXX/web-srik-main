{
  "name": "web-srik-main",
  "version": "1.0.0",
  "description": "SRIK Web Platform",
  "private": true,
  "scripts": {
    "seed-admin": "ts-node scripts/seed-admin.ts",
    "dev": "pnpm --parallel run dev",
    "build": "pnpm --parallel run build",
    "lint": "pnpm --parallel run lint"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
